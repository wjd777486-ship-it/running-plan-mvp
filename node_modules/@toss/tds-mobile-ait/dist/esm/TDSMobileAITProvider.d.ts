import { TDSMobileProvider } from '@toss/tds-mobile';
import { type ComponentProps } from 'react';
export type TDSMobileAITProviderProps = Omit<Partial<ComponentProps<typeof TDSMobileProvider>>, 'userAgent'> & {
    userAgent?: string;
    /**
     * 더큰텍스트 지원 여부.
     * 기본값은 false로 더큰텍스트 설정을 따르지 않아요.
     */
    fontScaleAvailable?: boolean;
    /**
     * 브랜드 기본 색상.
     * 별도로 지정하지 않은 경우 granite.config.ts에 정의된 브랜드 기본 색상을 사용해요.
     */
    brandPrimaryColor?: string;
};
export declare function TDSMobileAITProvider({ userAgent: stringUserAgent, fontScaleAvailable, children, brandPrimaryColor: brandPrimaryColorProp, ...props }: TDSMobileAITProviderProps): import("react/jsx-runtime").JSX.Element;
